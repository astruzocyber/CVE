# new_alerts.json is regenerated every run and only used transiently to drive
# notifications within the same job -- not meant to be committed.
docs/data/new_alerts.json

__pycache__/
*.pyc
.venv/
venv/
