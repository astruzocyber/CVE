{
  "consecutive_no_improvement": 0,
  "consecutive_failed_cycles": 0,
  "total_cycles": 5,
  "implemented": [
    {
      "cycle": 1,
      "timestamp": "2026-09-09T02:00:00Z",
      "title": "Retry with backoff on CISA KEV and EPSS fetch failures",
      "description": "fetch_kev() and fetch_epss() previously made a single bare HTTP attempt each, unlike nvd_query() which already retried 3x with exponential backoff. A transient network blip, 429, or 5xx from either CISA KEV or FIRST.org EPSS would silently zero out KEV status (losing the +25 risk-score bonus for every alert that run) or EPSS scores (40% of the composite risk score) for the whole run, degrading risk-score accuracy without any visible failure. Both functions now use the same 3-attempt exponential backoff pattern already established for NVD, with explicit stderr logging of each retry attempt. Zero new dependencies, zero new API calls under normal conditions (retries only fire on actual failure).",
      "commit_sha": "f4428be"
    },
    {
      "cycle": 2,
      "timestamp": "2026-09-09T02:38:03Z",
      "title": "Retry with backoff on Dependabot Alerts API pagination fetch",
      "description": "fetch_dependabot_alerts() was the last remaining HTTP call in the pipeline still making a single bare attempt per page, unlike fetch_kev()/nvd_query()/fetch_epss() which all already retry 3x with exponential backoff on 429/5xx/transient errors after cycles 1's fix. Because dependabot_repos includes astruzocyber/CVE itself (actively used, not a dead config path), a transient failure on any page mid-pagination would silently truncate or drop that repo's Dependabot alerts for the entire run with no retry. Added the same 3-attempt backoff pattern (429/5xx retried, other HTTP errors and exhausted retries logged and treated as end-of-pagination for that repo, matching prior behavior on unrecoverable failure). Zero new dependencies, zero added call volume under normal conditions.",
      "commit_sha": "8e94e2d"
    },
    {
      "cycle": 3,
      "timestamp": "2026-09-09T03:20:57Z",
      "title": "Retry with backoff on GHSA advisories fetch",
      "description": "fetch_ghsa_advisories() was the last remaining external HTTP call in the pipeline still making a single bare attempt per package, after cycles 1-2 already added the same 3-attempt exponential-backoff pattern to fetch_kev()/nvd_query()/fetch_epss()/fetch_dependabot_alerts(). Even though ghsa_packages is currently empty in config/watchlist.yaml (so the function is a no-op today), it's live production code that will run the instant the user adds a package name, and leaving it as the sole unretried call was an inconsistency waiting to bite silently. Added the identical 429/5xx-retried, other-HTTP-errors-logged-and-skipped pattern used everywhere else in the file. Zero new dependencies, zero added call volume under normal conditions (retries only fire on actual transient failure), completes the reliability layer across every external source the pipeline touches.",
      "commit_sha": "d92b1de"
    },
    {
      "cycle": 4,
      "timestamp": "2026-09-09T03:52:00Z",
      "title": "Frontend accessibility: labels and aria-attributes on filter controls",
      "description": "The dashboard's search input, three select filters (KEV status, source, sort order), and the per-card risk-score breakdown toggle button had no accessible name for screen readers -- inputs relied only on a visual placeholder and unlabeled <select> elements, and the toggle button never communicated its expanded/collapsed state via aria-expanded. Added a <label class=\"visually-hidden\"> (new CSS utility class, standard clip-based visually-hidden pattern) paired with each control plus matching aria-label attributes, added role=\"status\" to the empty-state message and aria-live=\"polite\" to the result count so screen-reader users get live updates on filter changes, and wired aria-expanded=\"true\"/\"false\" on the score-breakdown toggle button so its state is announced. Pure frontend markup/CSS change, zero new dependencies, zero risk to data pipeline or existing functionality -- purely additive attributes with no visual change (visually-hidden labels are clipped off-screen).",
      "commit_sha": "23a0e81"
    },
    {
      "cycle": 5,
      "timestamp": "2026-09-09T04:30:13Z",
      "title": "Retry with backoff on GitHub Issue creation (notify_github_issues.py)",
      "description": "create_issue() in scripts/notify_github_issues.py was the last remaining external HTTP call in the pipeline still making a single bare attempt, after prior cycles added the same 3-attempt exponential-backoff pattern to fetch_kev()/nvd_query()/fetch_epss()/fetch_dependabot_alerts()/fetch_ghsa_advisories() in scripts/aggregate.py. A transient 429/5xx from the GitHub Issues API would silently drop a vulnerability notification for the rest of the run with no retry -- notably worse than a data-fetch failure since notify_github_issues.py only reads new_alerts.json once and never re-runs for those alerts (idempotency design already accepts 'seen but not notified' as a rare tradeoff, but this closes the transient-failure gap that was causing it unnecessarily). Added the identical 429/5xx-retried, other-HTTP-errors-logged-and-skipped pattern used throughout aggregate.py. Zero new dependencies, zero added call volume under normal conditions, completes the reliability layer across every external HTTP call in the entire pipeline (both aggregate.py and notify_github_issues.py).",
      "commit_sha": "4b537d1"
    }
  ],
  "rejected": [],
  "last_cycle_at": "2026-09-09T04:30:13Z",
  "stopped": false
}
